import styles from './playlist-card.module.scss'

const PlaylistCard = ({ playlist }) => {
    return (<div className={styles['playlist-card']}>
        card
    </div>)
}

export default PlaylistCard